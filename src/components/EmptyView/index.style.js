import { colors, size, theme } from '../../theme';

export default {
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  tip: theme.styles.tipTitle,
  subTip: theme.styles.tipSubTitle,
}

import { colors, size } from './variables';

export default {
  tipTitle: {
    fontSize: size.lg,
    padding: 5,
    color: colors.title,
  },
  tipSubTitle: {
    fontSize: size.md,
    color: colors.text,
  }
}

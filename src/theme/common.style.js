import { Dimensions } from 'react-native';

export const weight = {
  bold: '500',
  bolder: '700',
};

export const size = {
  xs: 10,
  sm: 12,
  md: 14,
  ml: 17,
  lg: 20,
  xlg: 24,
};

export const { width, height } = Dimensions.get('window');